import FundadorChip from "@/components/FundadorChip";

/** Línea de confianza en Hero — avatar mini + texto; abre el panel al clic. */
export default function HeroTrustLine() {
  return <FundadorChip variant="hero" />;
}
